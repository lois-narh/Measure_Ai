import 'package:flutter/material.dart';

class ClientMeasurementPage extends StatelessWidget {
  const ClientMeasurementPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
    );
  }
}